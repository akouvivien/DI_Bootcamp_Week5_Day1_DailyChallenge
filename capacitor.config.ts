import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'DI_Bootcamp_Week5_Day1_DailyChallenge',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
